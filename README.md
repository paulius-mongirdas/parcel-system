## Frontend:
```
npm install
npm start
```
## Backend:
```
npm install prisma 
npm add -D prisma
npm add @prisma/client
npm add class-validator
npm add class-transformer
npm install

npm run start:dev
docker compose up parcel-system-db -d

npx prisma migrate dev
npx prisma studio
```
